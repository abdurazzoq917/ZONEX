{
  "name": "zonex-online",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "start": "node local-server.js",
    "dev": "node local-server.js",
    "kv": "node scripts/kv-check.js",
    "build:native": "node scripts/build-native-www.js",
    "cap:sync": "npm run build:native && npx cap sync android",
    "android:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}

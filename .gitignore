# Sirlar — hech qachon GitHub'ga tushmasin
.env
.env.local
.env.*.local

node_modules/
.vercel/
npm-debug.log*

# Lokal server bazasi (local-server.js shu yerga yozadi) — haqiqiy
# o'yinchi ma'lumotlari (ism, rasm, joylashuv) bo'lishi mumkin,
# git'ga tushmasin.
world.json

# Native (Capacitor) build artifacts — root fayllardan build:native
# skripti bilan qayta yaratiladi, git'ga tushmaydi.
/public/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/*.keystore
android/app/*.jks
